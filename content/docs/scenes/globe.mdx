---
title: Globe
description: A dotted globe that rotates slowly with two orbit rings. The dots use --primary and the rings blend --primary with --muted-foreground. Fully theme-awa
---

A dotted globe that rotates slowly with two orbit rings. The dots use `--primary` and the rings blend `--primary` with `--muted-foreground`. Fully theme-aware and offline.

<ScenePlayground slug="globe" />
